Name => Dipesh Patidar
Set => B

Q1. => select * from Customers order by name;

Q2. => select * from Products order by cost desc limit 10;

Q3. => select salary from Employees order by salary limit 1,1;

Q4. => select count(*) from Customers group by city;

Q5. => select dept_id, sum(salary) from employees group by dept_id having sum(salary) > 200000;

Q6. =>  select right(phone_number ,4), substring(customerName,1,3) from Customers;

Q7. => select * from Products where product_name like "%Pro%";

Q8. => select course, count(*) from Students group by course having count(*) >= 30;

Q9. => select count(*) as "Number of orders" from orders group by year(o_date),month(o_date);

Q10. => select dept_id, max(salary) as "highest salary", min(salary) as "lowest salary" from Employees group by dept_id;

Q11. => a

Q12. => a

Q13. => a

Q14. => b

Q15. => b

Q16. => select c.customer_id, c.customer_name, avg(o.total_amount) as "avg amount" from Customers c
	inner join orders o 
	on c.customer_id = o.customer_id
	group by c.customer_id,c.customer_name
	having count(*) >= 2;

Q17. => select p.product_id, p.product_name, count(*) from products p
	inner join sales s
	on p.product_id = s.product_id
	group by p.product_id, p.product_name
	having count(*) >=50;



